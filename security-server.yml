server:
  port: ${port:  port:
}
  error.whitelabel.enabled: false

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka/
  server:
    waitTimeInMsWhenSyncEmpty: 0
spring:
  datasource:
    url: ${app.datasource.url}
    username: swiftleap
    password: swiftleap
    driver-class-name: com.mysql.jdbc.Driver
  social:
    facebook:
      clientId: 1307552222602859
      clientSecret: 004e35ef68894981197d2297e7639870
    google:
      clientId: xx
      clientSecret: xx
logging:
  level:
    root: ${app.root.logLevel}
    org.springframework.web: ${app.springframework.web.logLevel}
    org.hibernate: ${app.hibernate.logLevel}
flyway:
  baselineOnMigrate: true
